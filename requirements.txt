# HTTP & scraping
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.3
lxml>=5.2.1
playwright>=1.44.0
# cloudscraper bypasses Cloudflare's basic bot-detection (TLS fingerprint / JS
# challenge) without launching a full browser — used as a middle tier between
# plain httpx and the Playwright fallback in agents.scrapers.base.HttpClient.
cloudscraper>=1.2.71

# Data models & validation
pydantic>=2.7.1
pydantic-settings>=2.2.1

# AI / LLM
openai>=1.30.1

# Scheduling
schedule>=1.2.2

# Email
python-dotenv>=1.0.1

# Utilities
tenacity>=8.3.0
python-dateutil>=2.9.0
diskcache>=5.6.3

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-mock>=3.14.0
responses>=0.25.0
